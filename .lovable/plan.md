

Two small changes in `src/pages/Index.tsx`:

1. **Change "Verified Buyer" text color** from `text-cta` (gold) to green (`text-green-500`) — both the `BadgeCheck` icon and the span text.

2. **Replace the testimonials section headline** ("4.9 out of 5 Stars") with `"Join 47,000+ Men Who've Restored Their Sexual Power"` and move the aggregate rating below it as a subtitle.

